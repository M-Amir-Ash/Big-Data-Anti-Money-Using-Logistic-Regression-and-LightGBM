# Raw Data

Download the Kaggle dataset and place the extracted CSV file in this folder.

```bash
kaggle datasets download -d berkanoztas/synthetic-transaction-monitoring-dataset-aml
```

Accepted default filenames include `SAML-D.csv`, `SAML_D.csv`, `saml-d.csv`, and `saml_d.csv`.
